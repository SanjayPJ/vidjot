<div class="card mt-3">
  <div class="card-body">
    <h3 class="pb-2"><strong>Add Video Idea</strong></h3>
    {{#each errors}}
    <div class="alert alert-danger" role="alert">
      {{ text }}
    </div>
    {{/each}}
    <form action="/ideas" method="post">
      <div class="form-group">
        <label for="exampleInputEmail1">Title</label>
        <input name="title" required value="{{ title }}" type="text" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
      </div>
      <div class="form-group">
        <label for="exampleInputEmail1">Details</label>
        <textarea required name="details" class="form-control" id="exampleFormControlTextarea1" rows="3">{{ details }}</textarea>
      </div>
      <button class="btn btn-primary">Submit</button>
    </form>
  </div>
</div>